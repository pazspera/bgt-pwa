import { it, vi, describe, expect, afterEach } from "vitest";


describe("collectionApiService: getCollections()", ()=> {
  it.only("success: returns list of all boardgames", async()=> {
    // crear archivo mockeando respuesta
    // se tiene que llamar al método 1 con get
    // tiene que devolver la lista de boardgames
  });

  it.todo("error: resource not found (404)", async()=> {});
  it.todo("error: internal server error (500)", async()=> {});
  it.todo("error: network error", async()=> {});
})