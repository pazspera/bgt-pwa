import { it, expect, describe } from "vitest";


describe("image rendering", ()=> {

  it.todo("displays logo image", ()=> {
    // img esté presente
  });

  it.todo("applies correct css class to image", ()=>{
    // que la imagen tenga .navbar-icon
  });

  it.todo("correct src attribute on the image", ()=>{
    // que el src sea favicon.ico
  });
});

it.todo("renders the correct :to route on the link", ()=> {});

